#!/bin/bash

set -euo pipefail

pushd build/src/packages/flatpak
    flatpak-builder --force-clean --user --install-deps-from=flathub --repo=repo builddir io.github.NEVAIL_AUTHOR_STRIPPED.NEVAIL_APP_NAME_STRIPPED.yml
    flatpak build-bundle repo ../../../../NEVAIL_INTERNAL_APP_NAME.flatpak io.github.NEVAIL_AUTHOR_STRIPPED.NEVAIL_APP_NAME_STRIPPED --runtime-repo=https://flathub.org/repo/flathub.flatpakrepo
popd
